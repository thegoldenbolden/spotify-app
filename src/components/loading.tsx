import Layout from '../layouts';

const Loading = () => {
 return (
  <Layout>
   <span className="grid w-full h-full place-content-center">Loading...</span>
  </Layout>
 );
};
export default Loading;
